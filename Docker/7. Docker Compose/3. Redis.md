

```yaml
version: '3.9'

services:
  redis:
    image: redis:7
    container_name: redis_cache
    restart: always
    ports:
      - "6379:6379"
```

👉 Redis mặc định không cần password (có thể thêm sau)

---